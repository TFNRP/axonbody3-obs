const config = {
  // change the camera id. leave blank for an automatically generated one
  CustomCameraId: '',
  // override the UTC offset by x hours
  ForceUTCTimeZoneOffset: false,
  // during daylight saving time, adds +1 to the UTC offset
  CalculateDaylightSavingsTime: true,
  // volume of the beep. 1 = 100%
  BeepVolume: 1,
}